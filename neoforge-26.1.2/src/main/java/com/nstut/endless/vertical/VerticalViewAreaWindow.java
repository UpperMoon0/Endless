package com.nstut.endless.vertical;

/** Exposes the live sparse vertical render-window geometry to renderer mixins. */
public interface VerticalViewAreaWindow {
    int endless$getWindowBaseSection();

    int endless$getWindowSectionCount();
}
